@model IEnumerable<LibraryApp.Web.Models.Book>
@inject IHttpContextAccessor HttpContextAccessor

@{
    // Le titre de la page sera "Livres de genre : [Nom du genre]"
    ViewData["Title"] = $"Livres de genre : {ViewBag.CurrentGenre}";
    var userId = HttpContextAccessor.HttpContext?.Session.GetString("UserId");
}

<div class="row align-items-center mb-4">
    <div class="col-md-8">
        <h2 class="mb-0">📚 Livres de genre : <span class="text-primary">@ViewBag.CurrentGenre</span></h2>
        <p class="text-muted">Découvrez notre sélection d'ouvrages dans le genre "@ViewBag.CurrentGenre".</p>
    </div>
    <div class="col-md-4 text-md-end">
        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Retour à l'accueil</a>
        <a asp-action="Index" class="btn btn-info">Voir tout le catalogue</a>
    </div>
</div>

<div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4 mb-5">
    @if (Model != null && Model.Any())
    {
        @foreach (var book in Model)
        {
            <div class="col">
                <div class="card h-100 shadow-sm border-0">
                    @if (!string.IsNullOrEmpty(book.CoverImageUrl))
                    {
                        <img src="@book.CoverImageUrl" class="card-img-top" alt="Couverture de @book.Title" style="height: 250px; object-fit: cover;">
                    }
                    else
                    {
                        <div class="d-flex align-items-center justify-content-center bg-light text-muted" style="height: 250px;">
                            <span>Pas d'image</span>
                        </div>
                    }
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title text-primary">@book.Title</h5>
                        <p class="card-text text-muted mb-1">@book.Author</p>
                        <p class="card-text text-muted mb-3">@book.PublicationYear</p>
                        <a asp-action="Details" asp-route-id="@book.Id" class="btn btn-sm btn-outline-info mt-auto">Détails</a>
                        
                        @* Optionnel: Boutons Emprunter/Retour si vous voulez les afficher ici aussi *@
                        @* Exemple (doit être adapté avec votre logique de prêt) : *@
                        @* @if (book.IsAvailable && userId != null)
                        {
                            <form asp-controller="Books" asp-action="Borrow" method="post" class="d-inline mt-2">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="bookId" value="@book.Id" />
                                <input type="hidden" name="userId" value="@userId" />
                                <button type="submit" class="btn btn-sm btn-success">Emprunter</button>
                            </form>
                        } *@
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <div class="col-12">
            <p class="text-muted">Aucun livre trouvé pour le genre "@ViewBag.CurrentGenre" pour le moment.</p>
            <p>Essayez de vérifier l'orthographe ou d'ajouter de nouveaux livres à ce genre.</p>
        </div>
    }
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
}